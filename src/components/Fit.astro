---
import { landing } from '../content/landing';
---

<section class="section-shell fit-section">
  <h2 data-text-reveal>{landing.fit.title}</h2>
  <div class="fit-list">
    {landing.fit.items.map((item) => <p>{item}</p>)}
  </div>
</section>
